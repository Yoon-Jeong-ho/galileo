# Generated content (checked in via sources elsewhere)
# We keep most generated files out of git, but we *do* version the small, paper-ready
# LaTeX snippet(s) that the draft \input{}s directly.
generated/
!generated/table1_rows.tex

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.toc
*.synctex.gz
*.synctex(busy)
*.pdf
